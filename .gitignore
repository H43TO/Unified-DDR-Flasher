# ── Private sources — never publish ─────────────────────────────────────────

# Core library source (private)
lib/SPDToolLibrary.cs
lib/SPDToolLibrary.csproj
lib/UDF-Core.csproj
lib/*.snk

# Firmware source (private)
fw/

# ── Build artefacts ───────────────────────────────────────────────────────────
bin/
obj/
*.user
*.suo
.vs/

# Pico SDK / CMake build directory
fw/build/

# ── Per-developer local config (contains machine-specific paths) ──────────────
src/UDFCore.local.props
src/SPDToolLibrary.local.props

# ── NuGet ────────────────────────────────────────────────────────────────────
packages/
*.nupkg
!packages/README.md

# ── OS noise ─────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Release build output (not committed; attached to GitHub Release) ──────────
# firmware/*.uf2 and firmware/*.bin ARE committed (pre-built for end users).
# lib/prebuilt/UDF-Core.dll IS committed (pre-built for end users to compile against).
# Do not add those paths here.
/docs/INTERNAL_DEV.md
/private_docs
/lib/*.cs
/lib/prebuilt/obfuscated
/lib/obfuscar.xml
/lib/packages.config
/tools
